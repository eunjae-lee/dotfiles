// Zed keymap
//
// For information on binding keys, see the Zed
// documentation: https://zed.dev/docs/key-bindings
//
// https://github.com/zed-industries/zed/blob/main/assets/keymaps/default-macos.json
//
// To see the default key bindings run `zed: open default keymap`
// from the command palette.
[
  {
    "bindings": {
      "ctrl-shift-alt-cmd-enter": "workspace::ToggleZoom"
    }
  },
  {
    "context": "Workspace",
    "bindings": {
      "alt-t": "terminal_panel::ToggleFocus"
      // "shift shift": "file_finder::Toggle"
    }
  },
  {
    "context": "Editor",
    "bindings": {
      // "j k": ["workspace::SendKeystrokes", "escape"]
      "cmd-e": "projects::OpenRecent",
      "shift-cmd-n": "workspace::NewWindow"
      // "alt-cmd-c": "workspace::CopyRelativePath"
    }
  },
  {
    "context": "Editor",
    "bindings": {
      "alt-cmd-c": ["task::Spawn", { "task_name": "Copy: Relative Path:Line" }],
      "ctrl-alt-cmd-c": [
        "task::Spawn",
        { "task_name": "Copy: Relative Path:Line + Code" }
      ],
      "shift-cmd-t": [
        "task::Spawn",
        { "task_name": "[calcom] Run test for this file" }
      ]
    }
  },
  {
    "context": "Terminal",
    "bindings": {
      // "j k": ["workspace::SendKeystrokes", "escape"]
      "cmd-e": "projects::OpenRecent"
    }
  }
]
